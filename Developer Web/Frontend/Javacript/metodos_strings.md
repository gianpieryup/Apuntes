````javascript
.length     // Longitud
.match      // Retorna la posicion, si no hay return -1
.slice		// Corta un string y retorna uno nuevo
//Ejemplo lista.slice(2,60)

.trim()    // Quita los espacios en blanco al principio y al final
.split(valor) // Toma un string y lo divide de acuerdo al 'valor'
.replace('str a buscar','str a reemplazar')
.toString() // Transforma todo a un strind
// 2 -> "2"  |   ["as","df"] -> "asdf"
````

